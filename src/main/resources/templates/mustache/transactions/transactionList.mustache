<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Transactions</title>
    <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>

<div>

    <h1>TRANSACTIONS</h1>

    <div>
        <ul>
            {{#.}}
            <li>{{id}} - <a href='/users/{{from.id}}'>{{from.name}}</a> gives <a href='/transactions/{{id}}'>{{amount}} bitcoins</a> to
                <a href='/users/{{to.id}}'>{{to.name}}</a></li>
            {{/.}}
        </ul>
    </div>

</div>

</body>
</html>